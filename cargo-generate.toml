[template]
name = "Rust Workspace Starter"
description = "Workspace with one binary and one library crate"
version = "0.1.0"

[placeholders]
lib_crate = { type = "string", prompt = "Name of the library crate" }
bin_crate = { type = "string", prompt = "Name of the binary crate" }
